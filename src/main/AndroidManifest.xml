<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="${appName}"
        android:supportsRtl="true">

        <!-- Required: Mihon discovers extensions via this meta-data -->
        <meta-data
            android:name="tachiyomi.extension.class"
            android:value="${extClass}" />
        <meta-data
            android:name="tachiyomi.extension.factory"
            android:value="${extFactory}" />
        <meta-data
            android:name="tachiyomi.extension.nsfw"
            android:value="${nsfw}" />
        <meta-data
            android:name="tachiyomi.extension.hasSettings"
            android:value="${isConfigurable}" />

        <!-- Required: Mihon scans for this action to find extensions -->
        <receiver
            android:name=".NineGridReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="eu.kanade.tachiyomi.extension.EXTENSION" />
            </intent-filter>
        </receiver>

        <!-- Optional: Deep link handler -->
        <activity
            android:name=".NineGridUrlActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="9grid.cc"
                    android:pathPrefix="/series/"
                    android:scheme="https" />
            </intent-filter>
        </activity>
    </application>

</manifest>
